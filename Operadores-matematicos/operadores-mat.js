/* 
OPERADORES MATEMÁTICOS:
resta
multiplicación
división
suma 

*/

let suma = 150 + 26;
let resultadoSuma = `

	<h2>el resultado de la suma es:
	${suma}
	<h2/>

`;

document.write(resultadoSuma);

let resta = 150 - 26;
let resultadoResta = `

	<h2>el resultado de la resta es:
	${resta}
	<h2/>

`;

document.write(resultadoResta);

let multi = (150 * 26);
let resultadoMulti = `

	<h2>el resultado de la multiplicación es:
	${multi}
	<h2/>

`;

document.write(resultadoMulti);

let division = (150 / 26);
let resultadoDivi = `

	<h2>el resultado de la división es:
	${division}
	<h2/>

`;

document.write(resultadoDivi);